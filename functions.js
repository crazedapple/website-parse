/**
 * Функция, которая берет входные числа и складывает их.
 **/
module.exports = {
    /*sumNumbers: function(array) {
        let sum = 0
        array.forEach( num => {sum = sum + num} )
        return sum
    }*/
}